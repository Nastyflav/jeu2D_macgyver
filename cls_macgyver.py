#from constants file

#MacGyver class

    #character initialization (def __init__)
        #sprites character
        #position character
        #default direction

    #character moves
        #if right direction
            #if MC stays on screen
                #if destination square isn't a wall
                    #one square move
                    #position

        #if left direction
            #if MC stays on screen
                #if destination square isn't a wall
                    #one square move
                    #position
        #if up direction
            #if MC stays on screen
                #if destination square isn't a wall
                    #one square move
                    #position

        #if down direction
            #if MC stays on screen
                #if destination square isn't a wall
                    #one square move
                    #position

